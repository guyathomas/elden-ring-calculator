
  # Elden Ring Weapon Calculator UI

  React-based web UI for the Elden Ring Weapon Calculator.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
